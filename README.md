# Random number generator
1. **Download the script**  
   Download the file `rng.sh` and make it executable.

2. **Run the script**
   Run the script and add exactly two arguments (the minimal and the maximal possible number).
